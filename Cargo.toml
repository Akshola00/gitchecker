[package]
name = "gitchecker"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.45", features = ["derive", "cargo"] }
chrono = { version = "0.4", features = ["serde"] }
git2 = "0.20.2"
